name: UI Tests

on:
  push:
  pull_request:
  workflow_dispatch:
    inputs:
      suite:
        description: "TestNG suite file to run"
        required: true
        default: "testng.xml"
        type: choice
        options:
          - testng.xml
          - smoke.xml

jobs:
  test:
    runs-on: ubuntu-latest            # Chrome and Firefox are already installed on GitHub runners

    # The same suite runs on each browser. This is the evidence of cross-browser runs:
    # one job, and one report, per browser.
    strategy:
      fail-fast: false                # a failure on one browser must not cancel the other
      matrix:
        browser: [chrome, firefox]

    name: ${{ matrix.browser }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      # The full suite, not smoke: smoke is 37 of the 57 runs, and the 20 it leaves out are the ones most
      # likely to catch the live site changing - the Gainers / Losers ordering, the protected routes and
      # most of the Why MultiBank checks. A green tick should mean the suite passes, not part of it.
      - name: Run tests (headless ${{ matrix.browser }})
        run: >
          mvn -B clean test
          -Dsuite=${{ github.event.inputs.suite || 'testng.xml' }}
          -Dheadless=true
          -Dbrowser=${{ matrix.browser }}

      - name: Upload Extent report
        if: always()                  # upload the report even when tests fail
        uses: actions/upload-artifact@v4
        with:
          name: extent-report-${{ matrix.browser }}
          path: reports/
